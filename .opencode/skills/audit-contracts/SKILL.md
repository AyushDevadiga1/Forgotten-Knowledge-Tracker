---
name: audit-contracts
description: Cross-boundary procedure for finding API, schema, storage, and caller-consumer mismatches.
---

Trace values across boundaries and compare both sides using source code, schemas, types, tests, and explicit specs. Identify what each side assumes and where those assumptions diverge.

A candidate is not confirmed until its divergence produces a concrete incorrect outcome or violates an explicit contract.
