# P-003 — ORM model vs migration DDL drift (learning_items.last_review_date)

- PATTERN_ID: P-003
- ROOT_CAUSE_PATTERN:
  Column exists in migration DDL (005_learning_item_last_review, migrations.py:82-85)
  but is absent from the ORM model (db/models.py:122-149). Because startup only runs
  create_all (P-002), create_all-provisioned DBs lack the column while migrated DBs
  have it — two divergent live schema variants of the same table.
- ORIGIN_FINDING: FKT-F-003
- SEARCH_METHOD:
  - Read-only sqlite probe (`C:\Users\hp\AppData\Local\Temp\opencode\pminer_schema_probe.py`,
    `sqlite:///tracker_app/data/sessions.db?mode=ro`) dumped all 16 tables'
    columns/indexes and the applied-migrations list.
  - Diffed live schema vs db/models.py vs db/migrations.py DDL (001-010).
  - Live DB: all 10 migrations applied; learning_items.last_review_date TEXT
    present (migration-005-only, absent from model).
- CANDIDATES: none.
- CONFIRMED_SIBLINGS:
  - None. Reverse direction (model-only columns) checked and cleared:
    prompted_at / window_title (007/008), repetitions / review_count / correct_count
    (009/010), attention_at_encoding / lambda_personalised (002) are all mapped by
    models.py AND added by migrations — consistent on both provisioning paths.
  - Type mismatches (prompted_at/window_title TEXT vs DateTime, models.py:177-180):
    SQLite type affinity makes them inert; no functional divergence.
- REJECTED_SIBLINGS:
  - schema_migrations.applied_at TEXT (migrations.py:143) — self-describing table
    with no ORM reader; format unconstrained; no drift.
- OPEN_QUESTIONS: none.
